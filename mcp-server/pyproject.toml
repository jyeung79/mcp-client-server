[project]
name = "mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.28.1",
    "uvicorn>=0.30.0",
]

[project.scripts]
weather-mcp = "weather:main"

[tool.setuptools]
py-modules = ["weather"]
